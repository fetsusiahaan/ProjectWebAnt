# https://www.robotstxt.org/robotstxt.html

User-agent: *
Allow: /

# Chat lives at /chat/<uuid> — one URL per visitor session, an unbounded
# space of private conversations. Neither the sessions nor the /chat
# redirect that mints them belong in an index.
Disallow: /chat
Disallow: /chat/

# Query strings never change what a page renders here, so crawling them
# only yields duplicates of the canonical URL.
Disallow: /*?

# Source maps.
Disallow: /assets/*.map$

Sitemap: https://fetsu.id/sitemap.xml
