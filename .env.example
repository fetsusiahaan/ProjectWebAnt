# OpenAI-compatible AI gateway. Both vars are bundled into the browser build
# (see envPrefix in vite.config.ts) — the key is PUBLIC, rotate if leaked.
IDU_API_KEY=your_idu_api_key_here
IDU_ENDPOINT=https://ai.intidatautama.com/v1
