name: Test Runner

on:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: self-hosted

    steps:
      - uses: actions/checkout@v4

      - name: Test Runner
        shell: powershell
        run: |
          Write-Host "Runner Connected!"
          hostname
          node -v
          npm -v
          git --version